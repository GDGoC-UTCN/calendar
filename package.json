{
  "name": "gdg-utcn-event-planner-2026-2027",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive GDG on Campus UTCN academic year event planner with React, Express, SQLite, Socket.IO and drag and drop.",
  "scripts": {
    "install-all": "npm install",
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "dev": "concurrently -n server,client -c auto \"npm run server\" \"npm run client\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": "20.x"
  }
}
